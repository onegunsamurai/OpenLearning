interface:
  display_name: "OpenLearning"
  short_description: "Repo-specific patterns and workflows for OpenLearning"
  default_prompt: "Use the OpenLearning repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true